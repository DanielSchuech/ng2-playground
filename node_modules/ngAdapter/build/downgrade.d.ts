import { UpgradeAdapter } from 'angular2/upgrade';
export declare class Downgrade {
    private upgradeAdapter;
    private module;
    constructor(upgradeAdapter: UpgradeAdapter, module: angular.IModule);
    directive(directive: any): Function;
    /**
     * returns Metadata of the Directive
     * i.e. selector, providers, host, ...
     */
    getMetadata(directive: any): any;
    /**
     * downgrade provided services which are given in the Directive Metadata
     */
    downgradeProviders(providers: Function[]): void;
    /**
     * create an empty scope with two way binding of the ng2 directive
     */
    createNewScopeWithBindings(directive: any): any;
}
