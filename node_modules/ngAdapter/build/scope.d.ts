import { ScopeEvents } from './scopeevents';
export declare class Scope {
    /**
     * setup scope with events, watch
     */
    static setUp(scope: any, scopeEvents: ScopeEvents): void;
    /**
     * scope watch function
     * saves the listeners and equals function for later check
     */
    static addScopeWatch(scope: any): void;
    /**
     * execute saved scope watch listener
     */
    static executeScopeWatchListener(property: string, scope: any, oldVal: any, newVal: any): void;
}
