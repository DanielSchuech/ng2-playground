import { UpgradeAdapter } from 'angular2/upgrade';
import { Type } from 'angular2/core';
export declare class Upgrade {
    private upgradeAdapter;
    private module;
    private addedProvider;
    private upgradedProviders;
    constructor(upgradeAdapter: UpgradeAdapter, module: angular.IModule, addedProvider: any, upgradedProviders: string[]);
    upgradeNg1Directive(directive: string): Type;
    /**
     * searches in angular.module for the directive and returns it
     */
    searchDirective(directive: string): Function | Array<any>;
    /**
     * searches for the directive function and the required dependencies
     * return {
     *  function: Function -> directive Function
     *  dependencies: string[] -> required Dependencies
     * }
     */
    evaluateDirectiveAndDeps(directive: Function | Array<string | Function>): {
        function: Function;
        dependencies: string[];
    };
    /**
     * create a directive
     */
    createDirective(selector: string, fn: Function, deps: string[]): any;
}
